/* Verified chapter/part YouTube URLs only. No URLs were supplied in the 50 DOCX files.
   Key format: "morada-chapter-part"; use part 0 for chapters without parts.
   Add URLs here when available; reading availability never depends on videos. */
window.LAS_MORADAS_VIDEOS = {};
